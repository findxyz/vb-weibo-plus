# 10 — chat.css 令牌收口与重复块去重（蔚蓝拟物）

**What to build:** 按 docs/design/DESIGN.md 把蔚蓝拟物皮肤缺失的令牌补成 CSS 变量（标题栏四段渐变、气泡描边、琥珀系状态色、玻璃高光阴影等），正文 hex 全部收口到变量；标题栏渐变块与关闭按钮块的多处重复声明合并为单一规则。纯等值替换，视觉零变化。

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] DESIGN.md 中已命名但未定义的 chat 侧取值全部成为 :root 变量并被引用
- [x] 标题栏渐变 + 内阴影只声明一次（含重叠元素的去重）
- [x] 对话框/弹层关闭按钮（含 ::before 与 hover）合并为一组规则
- [x] 两套皮肤不新增任何共享颜色变量（令牌不混用）
- [x] 浏览器对照 docs/design/style-guide.html 与原页面视觉一致

## Comments

- :root 从 13 个变量扩充到 75 个，新增项逐一对应 DESIGN.md「蔚蓝拟物」命名（标题栏四段渐变、气泡描边、琥珀系状态色、窗控/关闭钮渐变、侧栏与面板、输入与媒体等）；正文 hex 全部改为变量引用，白底统一走已有 --surface。
- 五处标题栏（主窗 + 三个对话框 + 两个弹层）的渐变与内阴影合并为单一规则，padding 差异单独一条；h2、标题图标、关闭按钮（含 ::before 与 hover）各自合并为一组选择器。
- 未入 DESIGN.md 的一次性取值（放大镜 #6b8193、图标投影 #074b9a、qr 占位 #f0f0f0 等）保留 hex，未新造令牌；两套皮肤无共享颜色变量。
- 验证：本地静态服务下用浏览器对 169 个元素做新旧样式表 computed-style 全量比对，过滤新增的 --* 定义后渲染属性零差异；两页截图目视正常。
