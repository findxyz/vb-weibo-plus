
## 事故：手动 javac 编译导致 -parameters 元数据丢失
- 原因：用 javac 手动重编译 PostController 时没带 -parameters，Spring @RequestParam 反射拿不到参数名，GET 接口全部 500
- 修复：带 -parameters 重新编译（已修复 target/classes 中的 class）
- 教训：此项目不要手动 javac 覆盖 target/classes，构建交给用户的 IDE；临时脚本 .scratch/gen-*.py 已按用户要求删除

## 修复混合发布微博视频不显示
- 根因：混合发布（多视频/图文混合）微博没有 page_info，视频在 mix_media_info.items 里，pic_ids 也是空的；旧代码只读 page_info，导致封面和视频地址都没采到
- 改动：新增 MixMediaInfoResponse（items: type + data，data 复用 PageInfoResponse 结构）；MblogResponse 加 mixMediaInfo 字段；PostMapper.toVideo 改为接收 MblogResponse，page_info 缺失时回退取 mix_media_info 第一个视频条目
- 测试：PostMapperTest 新增 falls_back_to_mix_media_video_when_page_info_missing，9 个测试全部通过
- 注意：数据模型只支持单视频，多视频只展示第一个；混合发布中的图片条目同样会被漏采（pic_infos 为空），本次未处理
- 存量数据不会自动补：如 Rc1O5FeSX 需删除后重新范围同步，或直接 UPDATE retweeted_json
- 测试运行方法：从 target/weibo-0.0.1-SNAPSHOT.jar 提取 BOOT-INF/lib 作为精确依赖 + junit 5.12.2/assertj 3.27.7/spring-test 6.2.19，JUnit Launcher 跑（本地 .m2 多版本共存，遍历组 classpath 必踩版本冲突）
- 遗留：.scratch/verify-lib 还有 54 个提取的 jar 副本未删（沙箱本轮批量删除限流）

## 添加博主按钮视觉修正
- .blogger-add 描边改为与 window-control 一致的白色 94% 描边，加号 SVG 加粗到 2.4
- 弹窗标题栏的 ➕ emoji 在 Windows 上渲染为黑色字形，改为白色 SVG 加号图标
- 上轮遗留的 .scratch/verify-lib 已分批删除完毕

## 重构 post 页面为线框草图风格
- 按根目录 index.html（Wireframe v0.1 手绘风格）重构了 src/main/resources/static/post/index.html 与 post.css
- post.css 整体替换为线框草图设计令牌（纸张底色、Patrick Hand/Caveat 手写字体、虚线边框、便签贴装饰）
- index.html 增加 Google Fonts 引用、wf-note 便签、wf-legend 图例、data-od-id 属性；标题改为「本地微博 · Wireframe v0.1」
- post.js 未改动，其使用的全部 class 均已在新 CSS 中覆盖
- 已同步到 target/classes/static/post/

## 去掉 post 页面的便签与胶带装饰
- 删除 index.html 中三张 wf-note 便签（panel · 01/02/03）
- 删除 post.css 中 .wf-note/.tape 样式、.post-retweet::before 胶带条、--note-yellow/--note-pink 令牌
- wf-legend 图例保留；已同步 target/classes/static/post/

## 去掉 wf-legend 图例与标题栏 WIREFRAME 徽章
- 删除 index.html 左下角 wf-legend 图例，页面标题改回「本地微博」
- 删除 post.css 中 .wf-legend 样式、h1#app-title::after 的 WIREFRAME v0.1 虚线徽章、对应 media query
- 已同步 target/classes/static/post/

## 图片加载失败隐藏碎图
- post.js 三处图片创建点加 onerror：微博图片与转发图片失败时隐藏整个格子（picEl.hidden），视频封面失败时只隐藏 img 保留播放占位
- 已同步 target/classes/static/post/post.js

## 修复碎图隐藏不生效
- 原因：.post-pic/.post-retweet-pic/.post-video img 的 display:block 覆盖了 hidden 属性的 UA 样式
- 修复：post.css 增加 .post-pic[hidden] 等显式 display:none 规则，已同步 target 并确认 8080 生效

## 修复「全部博主」行常驻黄色底色
- post.css 中 .blogger-row.all-bloggers 写死了 rgba(255,241,154,0.5) 背景，未选中时也像高亮态
- 改为默认透明，仅保留底部实线分隔；选中态走通用 .blogger-row.active 荧光笔高亮

## 删除根目录线框稿 index.html
- 其样式已完整迁移到 post 页面，根目录 index.html 已删除

## 新增同步历史微博功能（post 页面）
- 后端无需改动：直接复用已有 POST /post/range?uid&start&end（PostService.saveByRange，按天调 searchProfile，带 0.2-2s 随机 sleep 与全局锁）
- feed 头部新增「同步历史」按钮（仅选中单个博主时显示），点开手绘风 dialog：开始/结束日期（默认两年前~今天，与群聊页一致），提交后等待接口返回并显示 拉取/新增/已存在 统计，随后刷新日期时间轴
- post.css 将 .add-blogger 系列选择器扩展为与 .sync-history 共用，新增标题下载图标、sync-history-open 按钮、status.ok 非错误配色

## 修复历史同步 NPE：已删除的转发原微博
- /post/range 走搜索接口，已删除的原微博 retweeted_status 没有 user/id/created_at，PostMapper.toRetweetInfo 的 requireNonNull 抛 NPE 中断整个同步
- 修复：toRetweetInfo 对缺省字段用占位值兜底（postId/uid/createdAt=0，mblogId/screenName=空串），保留删除提示正文；前端显示 @未知用户
- Java 改动需用户在 IDE 重新构建生效（本机不手动 javac）

## 修正同步弹窗提示文案
- 用户确认：同步在服务端请求线程执行，关闭弹窗/页面不影响入库；tip 改为「同步在服务端执行，中途关闭弹窗不影响入库」

## 历史同步增加进度日志
- 原因：saveByRange 只在整个范围跑完后打一条日志，大范围同步过程中无任何输出
- 改动：PostService.saveByRange 开始时打「范围 start ~ end」，每天拉完且有条数时打一行进度（日期、拉取数、新增数）
- Java 改动，需用户 IDE 重新构建

## 修复 post.css 伪元素中文乱码
- 原因：Spring 以 text/css 返回时无 charset，浏览器对无声明编码的样式表按 Latin-1 解析，content:" · 来自 " 变 Â· æ¥è‡ª
- 修复：文件头加 @charset "UTF-8"，且 4 处非 ASCII content 改为 Unicode 转义（\00B7、\25B8、\25BE、\6765\81EA）
- chat.css 检查过无此问题

## 修复历史同步 SQLITE_BUSY
- 原因：群消息定时同步（每 20s 两个群）与 /post/range 并发写 SQLite；busy_timeout 管不到 WAL 快照冲突（SQLITE_BUSY_SNAPSHOT），直接抛异常
- 修复：PostService 对 upsertMetadata/capturePost/refreshLatestPostId 加 runWithBusyRetry/callWithBusyRetry（最多 5 次、0.5-2s 随机退避，沿 cause 链识别 SQLITE_BUSY）；application.yml busy_timeout 5000→30000
- Java 改动需用户 IDE 重新构建；application.yml 已同步 target/classes

## 还原 SQLITE_BUSY 重试实现（用户不接受该方案）
- 撤销 PostService 的 runWithBusyRetry/callWithBusyRetry 及所有调用点、Supplier import
- application.yml busy_timeout 还原为 5000
- 保留此前已接受的改动：toRetweetInfo 删除微博兜底、saveByRange 进度日志

## 同步弹窗改为发起后自动关闭
- submitSyncHistory 校验后直接 close 弹窗，runSyncHistory 后台 await /post/range，完成后刷新日期面板并在 dates-state 显示「同步完成：新增 N 条」
- 同步期间禁用「同步历史」入口按钮防重复发起
