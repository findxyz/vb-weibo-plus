# 2026-09-12

## JS 命名与结构重构（chat/post 全量）
- 文件命名标准：用域名名词命名，不要 common/helpers 杂货文件
  - conversation-session.js → sessions.js（createConversationSession → createSessions）
  - group-list.js → groups.js（createGroupList → createGroups）
  - chat-common.js 删除：compareMessages + 滚动锚点 → sessions.js（history/celebration 从 sessions import，全页只有这一种消息顺序定义）；MEDIA_TYPE → message-view.js；formatTime/formatDateTime 各自内联进唯一调用方（message-view/history 的模块私有函数）
  - post/helpers.js 删除：formatDate/isDateRangeValid → shared/date.js；showState/createVerifiedBadge → shared/dom.js（新文件）；createApiErrorHandler 内联进 post.js（唯一调用方），login.showLoginExpired 直接调用
- 教训：对同一文件的多个 Edit 不要在同一消息里并行发，会发生写竞争互相覆盖
- 已同步 target/classes/static 并删除旧文件；node --check 全部通过
- 注意：此前曾有一次重构只改了 target 没改 src，造成两边不一致；重构必须 src 优先再同步 target

## 前端规范审查（仅审查，未改代码）
- 已证实问题：post 页快速切换博主/日期存在请求竞态（posts.loadPosts、dates.loadDates 均无请求序号校验，onBloggerChanged 无防重入，search.jumpToPost 绕过 selectDate 守卫）；chat 页请求失败无 401 登录失效处理（post 页有 handleApiError，chat 只 console.warn，靠 60 次轮询兜底）
- 一致性问题：两页登录检测两种实现；historyOpen/imagePickerOpen 等按钮 disabled 由入口与模块两处写；qr-login 暴露无调用方的 stop；posts.revealPost 用 document.querySelector 而非模块容器
- 原生 fetch 核对结论：analysis(SSE)、history.capture(204)、dream(HTML 探测)、video-player(Range) 均合规，无需统一
- 未做：CSS 与 DESIGN.md 令牌全量对照、浏览器实测

## 审查问题全量修复（已实施）
- post 竞态：posts.loadPosts / dates.loadDates 加 loadVersion 序号，bloggers.onBloggerChanged 加 changeVersion 编排守卫；旧响应不再落地、不清新请求 loading
- chat 登录：新建 chat/login.js（与 post/login.js 同模式：qr-login + maybeCheck 60 次节拍周期检测 + showLoginExpired），chat.js 删除内联 qrLogin/checkLoginStatus/loginCheckTick；sessions/groups 新增 onAuthExpired 回调，401 时提示登录失效（含 initialize 的 401 分支）
- 按钮 disabled 归口：imagePickerOpen/videoPickerOpen 由 composer.refreshAvailability 单写（composer 开始返回 {refreshAvailability}），historyOpen/analysisOpen 由各模块 setGroup 单写，chat.js selectGroup 不再代写
- 小项：qr-login 移除无调用方的 stop；history capture→requestHistorySync、scroll 回调去掉 return loadMore；dates.selectFirstDate 统一走 toggleGroup；viewer 删除冗余 Esc 分支（原生 dialog 自带）；dream 探测加 confirmedUids 缓存；sessions.applyAttitudes 改一次遍历收集 renderedByMid；posts.revealPost 改用模块容器查询；post/index.html 删 5 个 data-od-id 残留
- 12 个 JS node --check 全过，已同步 target/classes/static（含新文件 chat/login.js、post/index.html）
- mattskills 本地与市场均未找到（此前安装未成功），本次按审查规范直接修复

## 入口实例命名统一（chat.js）
- 约定：工厂实例变量名 = 工厂名名词部分（posts = createPosts 模式，post.js 已全符合）
- chat.js 修正两处跑偏：conversation = createSessions → sessions；groupList = createGroups → groups
- 碰撞处理：initialize 局部 const groups → list；onGroupsChanged 箭头参数 groups → list；elements.conversation 是 DOM 属性（.conversation 容器），保留不动
- node --check 通过，已同步 target

## fire-and-forget Promise 统一加 void 标记
- 规则：async 函数内部已 try/catch（永不 reject）时，调用方不 await 是合法的，用 `void xxx()` 显式标记「有意不等待」消除 IDE 警告；这不是规范九禁止的「用 void 掩盖未处理异常」——前提是先确认被调函数内部有 catch
- chat 页：attitudes.load ×2（chat.js）、loadVisible 内（attitudes.js）、groups.refreshGroups/sessions.refresh（refreshView）、checkLoginStatus（login.js maybeCheck）、loadEarlierIfNeeded ×2（sessions.js）、loadMore ×2（history.js 滚动回调）
- post 页：login.checkLoginStatus（post.js 启动 + login.js onSuccess）、bloggers.loadBloggers（post.js 启动）、onBloggerChanged ×2（bloggers.js）、loadPosts ×2（posts.js selectDate/retry）
- 反例确认无需改：celebration.process、viewer 的 showPrevImage/showNextImage 均为同步函数
- 9 个文件 node --check 通过，已同步 target
